Simplified CalcService using Jersey (JAX-RS implementation) and Spring boot.

Rest service provider exposes it's API using Spring's @RestController & @RequestMapping annotations and
gets started with spring-boot-starter-web MVN dependency.

Integration test uses rest-assured for calling rest endpoint.